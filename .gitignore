# Ignore everything by default
*

# …but not directories (so folders appear on GitHub)
!*/

# Allow .gitkeep anywhere (so we can keep empty dirs)
!**/.gitkeep

# Allow these root files
!.gitignore
!README.md
!LICENSE

# Allow full content of these two folders
!Scripts/**
!old_scripts_dir/**

# (Optional) still ignore heavy/temporary stuff if it appears elsewhere
*.log
*.tar.gz
*.DS_Store
**/.ipynb_checkpoints/
**/events.out.tfevents.*
*.h5ad
*.zip
